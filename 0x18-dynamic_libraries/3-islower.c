#include "main.h"
#include <stdio.h>
/**
 * _islower - checks for lowercase char
 * @c: the char
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

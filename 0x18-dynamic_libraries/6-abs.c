#include "main.h"
#include <stdio.h>
/**
 * _abs - computes the absolute value
 * @n: the int
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
